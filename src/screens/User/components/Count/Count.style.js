import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  root: { textAlign: 'center', color: 'white' },
  count: { fontWeight: 'bold', fontSize: 20, margin: 0 },
  text: { fontSize: 12, margin: 0 }
}));
