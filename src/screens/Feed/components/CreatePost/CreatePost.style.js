import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  rootCreatePost: { display: 'flex', marginBottom: 16, padding: 16 },
  image: { width: 50, height: 50, borderRadius: '100%' },
  postForm: { width: '100%', marginLeft: 16, textAlign: 'right' },
  button: { marginTop: 16 }
});
